{
  "buildCommand": "cd frontend && npm install && npm run build && node ../scripts/postbuild.js",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
